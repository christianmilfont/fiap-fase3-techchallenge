apiVersion: v1
kind: Secret
metadata:
  name: evaluation-service-secret
  namespace: evaluation-service
type: Opaque
stringData:
  # Preencher após bootstrap: curl http://<LB-DNS>/auth/admin/keys
  SERVICE_API_KEY: PREENCHER_APOS_BOOTSTRAP
